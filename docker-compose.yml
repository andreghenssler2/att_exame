version: '3.8'

services:
  web:
    build: .
    container_name: app-web
    ports:
      - "3000:3000"
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: redis
    ports:
      - "6379:6379"